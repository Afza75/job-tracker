import React from 'react'
import ReactDOM from 'react-dom/client' // connects React code to the browser (HTML page)
import App from './App.jsx'
import './index.css'

ReactDOM.createRoot(document.getElementById('root')).render(<App />)
